---
title: 再看一眼 useState Hook
authors: [visualdust]
tags: [frontend, react, react-hooks]
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

之前说React Hooks可以没有class直接work。这是`useState`的写法和对应的class写法：

<!--truncate-->

<Tabs groupId="usestate-hook">
  <TabItem value="Example" default>

  ```typescript
  import React, { useState } from 'react';

  function Example() {
    // Declare a new state variable, which we'll call "count"
    const [count, setCount] = useState(0);
  
    return (
      <div>
        <p>You clicked {count} times</p>
        <button onClick={() => setCount(count + 1)}>
          Click me
        </button>
      </div>
    );
  }

  ```
  
  </TabItem>
  <TabItem value="Equivalent Class Example">

  ```typescript
  class Example extends React.Component {
    constructor(props) {
      super(props);
      this.state = {
        count: 0
      };
    }
  
    render() {
      return (
        <div>
          <p>You clicked {this.state.count} times</p>
          <button onClick={() => this.setState({ count: this.state.count + 1 })}>
            Click me
          </button>
        </div>
      );
    }
  }
  ```

  </TabItem>
</Tabs> 

